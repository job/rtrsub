function is_roa_covered_prefix()
prefix set roas;
{
     roas = [
        {% for item in data -%}
{{ item }}+
{%- if not loop.last -%},{%- endif -%}
{%- if loop.index % 4 == 0 -%}
{{ "\n        " }}
{%- else -%}
{{ " " }}
{%- endif -%}
{%- endfor %}
    ];

    if net ~ roas then {
        return true;
    }
    else return false;
}

function violates_roa()
{
{% for item in data -%}
{{ item.origins }}
{%- endfor %}
